{{#partial "headcode" }} {{/partial}} {{#partial "footercode" }} {{/partial}} {{#partial "content" }}
<div class="container min-height-container">
    <div class="form-group {{#if successMsg}}has-error{{/if}}">
        {{#if successMsg}}<span class="help-block dlxs-validation-error">{{successMsg}}</span>{{/if}}
    </div>
    <div class="row">
        <div class="col-xs-12">
            <ol class="breadcrumb">
                <li>Teachers</li>
                <li><a href="{{studentsUrl}}">Students</a></li>
                <li class="active">Student Details</li>
            </ol>
            <form action="{{formPostUrl}}" method="post">
                <input type="hidden" name="{{csrfParameterName}}" value="{{csrfToken}}" />
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">Student Details</h3>
                    </div>
                    <div class="panel-body">

                        {{#unless user.enabled}}
                            <div class="alert alert-danger">
                                <i class="fa fa-lock fa-2x"></i>&nbsp; Account is disabled.
                                Click <strong>Enable account</strong> below to activate user
                                account.
                            </div>
                        {{/unless}}

                        <div class="form-group {{#if emailError}}has-error{{/if}}">
                            <label for="email">{{#if emailError}}<i class="fa fa-times-circle-o"></i>{{/if}} Email customer </label>                            
                            {{#if emailError}}<span class="help-block dlxs-validation-error">({{emailError}})</span>{{/if}}
                            <input name="email" value="{{user.email}}" type="text" class="form-control" placeholder="Enter email">
                        </div>

                        <div class="form-group {{#if firstNameError}}has-error{{/if}}">
                            <label for="firstName">{{#if firstNameError}}<i class="fa fa-times-circle-o"></i>{{/if}} First name </label>
                            {{#if firstNameError}}<span class="help-block dlxs-validation-error">({{firstNameError}})</span>{{/if}}
                            <input name="firstName" value="{{user.firstName}}" type="text" class="form-control" placeholder="First name">
                        </div>

                        <div class="form-group {{#if lastNameError}}has-error{{/if}}">
                            <label for="lastName">{{#if lastNameError}}<i class="fa fa-times-circle-o"></i>{{/if}} Last name </label>
                            {{#if lastNameError}}<span class="help-block dlxs-validation-error">({{lastNameError}})</span>{{/if}}
                            <input name="lastName" value="{{user.lastName}}" type="text" class="form-control" placeholder="Last name">
                        </div>

                        <div class="form-group">
                            <label>College</label>
                            <p>{{college}}</p>
                        </div>

                        <div class="form-group">
                            <label>Group ({{#ifeqStr domain "MiniSevaIndia.com"}}Batch{{/ifeqStr}}{{#ifeqStr domain "MiniSevaLMS.com"}}Academic Year{{/ifeqStr}}, {{#ifeqStr domain "MiniSevaIndia.com"}}Standard{{/ifeqStr}}{{#ifeqStr domain "MiniSevaLMS.com"}}Grade{{/ifeqStr}}, Section)</label>
                            <p>{{group}}</p>
                        </div>

                        <div class="form-group {{#if mobilePhoneError}}has-error{{/if}}">
                            <label for="mobilePhone">{{#if mobilePhoneError}}<i class="fa fa-times-circle-o"></i>{{/if}} Mobile phone number </label>                            
                            {{#if mobilePhoneError}}<span class="help-block dlxs-validation-error">({{mobilePhoneError}})</span>{{/if}}
                            <input name="mobilePhone" value="{{user.mobilePhone}}" type="text" class="form-control" placeholder="Mobile phone number">
                        </div>
                    </div>
                    <div class="panel-footer">
                        <button type="submit" name="action" value="save" class="btn btn-primary">
                                <i class="fa fa-envelope-o"></i> Save changes</button>
                        <button type="submit" name="action" value="changePassword" class="btn btn-default">
                                <i class="fa fa-envelope-o"></i> Change password</button>
                        <button type="submit" name="action" value="cancel" class="btn btn-default">
                                <i class="fa fa-envelope-o"></i> Cancel</button>
                        {{#unless user.enabled}}
                            <button type="submit" 
                                    name="action" 
                                    value="enableAccount"
                                    class="btn btn-default rs-enable-account">
                                <i class="fa fa-recycle fa-lg rs-green"></i> Enable account</button>
                        {{else}}
                            <button type="submit" 
                                    name="action" 
                                    value="disableAccount"
                                    class="btn btn-default rs-disable-account">
                                <i class="fa fa-recycle fa-lg rs-red"></i> Disable account</button>
                        {{/unless}}
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<!-- /container -->
{{/partial}} {{> app/common/master }}