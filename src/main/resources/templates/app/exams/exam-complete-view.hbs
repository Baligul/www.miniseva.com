{{#partial "headcode" }}
{{/partial}}

{{#partial "footercode" }}
{{/partial}}

{{#partial "content" }}
<div class="container min-height-container">
    <div class="row">
        <div class="col-xs-12">
            <h1>{{title}}</h1>
            <p>{{successMessage}}</p>
            <p>Do you want to know your score?</p>
            <div>
                <a href="{{resultsUrl}}"
                class="btn btn-success">View My Exam Results</a>
                <a href="{{examsListUrl}}"
                class="btn btn-default">Return to Exams List</a>
            </div>
        </div>
    </div>
</div><!-- /container -->
{{/partial}}
{{> app/common/master }}
